/**
 * Shared input invariants enforced across business components.
 * Centralizes checks (e.g. mandatory non-blank fields) that several entities
 * apply uniformly so the rule and its HTTP semantics live in one place.
 */
package airhacks.confex.validations;
